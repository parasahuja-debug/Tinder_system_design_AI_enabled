# recommendation_service runtime deps. No JWT/bcrypt here: this service
# trusts the gateway's X-User-Id header and never handles tokens or
# passwords itself. It's read-only against the shared DB, so it needs
# SQLAlchemy Core (via the `text`/`bindparam` helpers) but not the ORM/
# declarative pieces the write-owning services use.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
prometheus-client==0.21.1 # /metrics endpoint for Prometheus scraping (Day 6)
