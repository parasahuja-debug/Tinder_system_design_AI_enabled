# matcher_consumer runtime deps. Same DB/observability stack as
# matcher_service, plus confluent-kafka for the actual consume/produce work
# (subscribing to `swipes`, publishing to `matches`).
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
prometheus-client==0.21.1
confluent-kafka==2.6.1
