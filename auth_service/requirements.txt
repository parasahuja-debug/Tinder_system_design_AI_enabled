# auth_service runtime deps — pinned so a rebuild is reproducible.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10   # Postgres driver for SQLAlchemy
pyjwt==2.10.1             # sign/verify the JWTs this service issues
bcrypt==4.2.1             # password hashing (called directly, not via passlib)
